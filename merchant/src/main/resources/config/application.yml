spring:
  datasource:
    url: jdbc:postgresql://localhost:5450/dev_emerchantpay
    username: dev_admin
    password: dev_password
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        default_schema: dev_merchant